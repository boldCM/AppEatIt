import React from "react";

import RecipeIngredients from "./RecipeIngredients";

export default {
  title: "Components",
  component: RecipeIngredients,
};

export const recipeIngredients = (args) => <RecipeIngredients {...args} />;
