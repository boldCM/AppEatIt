import React from "react";

import MenueButton from "./MenueButton";

export default {
  title: "Components",
  component: MenueButton,
};

export const menueButton = (args) => <MenueButton {...args} />;
