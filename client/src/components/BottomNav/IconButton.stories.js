import React from "react";
import IconButton from "./IconButton";

export default {
  title: "Example/Components/IconButton",
  component: IconButton,
};

export const iconButton = (args) => <IconButton {...args} />;
